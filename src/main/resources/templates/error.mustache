<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error {{status}}</title>
    <link rel="stylesheet" href="/css/error.css">
    <!-- Added FontAwesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
</head>
<body>
    <div class="error-container">
        <!-- Error icon -->
        <div class="error-icon">
            <i class="fas fa-exclamation-circle" style="font-size: 120px; color: var(--accent-color);"></i>
        </div>
        
        <!-- Error code -->
        <div class="error-status">{{status}}</div>
        
        <!-- Error messages -->
        <div class="error-text">
            <h2>{{error}}</h2>
            <p>{{message}}</p>
        </div>
        
        <!-- Container for buttons -->
        <div class="button-container">
            <a href="/products" class="back-button">
                <i class="fas fa-home"></i> Back to home
            </a>
            <a href="javascript:history.back();" class="report-button">
                <i class="fas fa-arrow-left"></i> Volver atrás
            </a>
        </div>
        
        <!-- Redirect counter -->
        <div class="redirect-counter">
            Redirecting to home page in <span class="redirect-countdown">10</span> seconds
        </div>
    </div>
    
    {{> common/footer}}
    
    <!-- Automatic redirect script -->
    <script src="/js/error.js"></script>
</body>
</html>