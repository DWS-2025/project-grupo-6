<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Detalles del Producto</title>
</head>
<body>

<h1>{{product.name}}</h1>
<p>{{product.description}}</p>
<p>Precio: ${{product.price}}</p>
<p>Stock: {{product.stock}}</p>
<img src="{{product.mainImage}}" alt="{{product.name}}" width="200">

<h2>Comentarios</h2>
<ul>
    {{#reviews}}
        <li>
            <strong>{{user}}:</strong> {{comment}} ({{rating}}/5)
            <!-- Botón de eliminar reseña -->
            <form action="/delete-review" method="post" style="display:inline;">
                <input type="hidden" name="productId" value="{{product.id}}">
                <input type="hidden" name="reviewId" value="{{id}}">
                <button type="submit">Eliminar Reseña</button>
            </form>
        </li>
    {{/reviews}}
</ul>

<h2>Añadir Comentario</h2>
<form action="/add-review" method="post">
    <input type="hidden" name="productId" value="{{product.id}}"> <!-- Asegúrate de enviar el productId -->
    <textarea name="comment" required></textarea>
    <select name="rating" required>
        <option value="1">1 ⭐</option>
        <option value="2">2 ⭐⭐</option>
        <option value="3">3 ⭐⭐⭐</option>
        <option value="4">4 ⭐⭐⭐⭐</option>
        <option value="5">5 ⭐⭐⭐⭐⭐</option>
    </select>
    <button type="submit">Enviar Reseña</button>
</form>

<br>
<a href="/products">
    <button>Volver a la lista de productos</button>
</a>

</body>
</html>

